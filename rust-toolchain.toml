[toolchain]
# 1.85 stabilized `edition2024`. PLAN.md v0.6 §7 / CLAUDE.md §4 require
# edition 2024 for parity with the absorbed aws/amazon-q-developer-cli-autocomplete
# crates (also resolver "3"). Bumping is harmless — rustup auto-downloads.
channel = "1.85"
components = ["rustfmt", "clippy"]
profile = "minimal"
